# E2E test mode: force the mock API so Playwright runs deterministically with no
# backend and no OpenAI calls. Empty VITE_API_BASE_URL means USE_MOCK is true.
# (This mode file overrides the local .env, which points at the dev backend.)
VITE_API_BASE_URL=
