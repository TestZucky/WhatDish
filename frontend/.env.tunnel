# Tunnel mode (used by `make tunnel`): talk to the backend through Vite's /api
# proxy on the same origin, so a single Cloudflare tunnel serves app + API.
# Empty base + proxy flag = real backend via same-origin /api (not mock).
VITE_API_BASE_URL=
VITE_API_PROXY=true
