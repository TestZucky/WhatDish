# Base URL of the WhatDish backend API.
# Leave empty to run the frontend against built-in mock data (no backend needed).
# Set it once the backend is running, e.g.:
#   VITE_API_BASE_URL=http://localhost:8000
VITE_API_BASE_URL=
